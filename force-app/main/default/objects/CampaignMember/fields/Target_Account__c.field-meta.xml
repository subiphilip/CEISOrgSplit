<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Target_Account__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Company this campaign is targeted for (if different than the contact&#39;s Account, for example: Contact Roles)</description>
    <externalId>false</externalId>
    <inlineHelpText>Company this campaign is targeted for (if different than the contact&#39;s Account, for example: Contact Roles)</inlineHelpText>
    <label>Target Company (Account)</label>
    <referenceTo>Account</referenceTo>
    <relationshipLabel>Campaign Members</relationshipLabel>
    <relationshipName>Campaign_Members</relationshipName>
    <required>false</required>
    <type>Lookup</type>
</CustomField>
