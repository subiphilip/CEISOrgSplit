<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Database_Check__c</fullName>
    <description>system.  contains the database ids that have contracts activated.  workflow will review the database id fields and determine if any need to be initialized for safari sync</description>
    <externalId>false</externalId>
    <inlineHelpText>system.  contains the database ids that have contracts activated.  workflow will review the database id fields and determine if any need to be initialized for safari sync</inlineHelpText>
    <label>Database Check</label>
    <length>30</length>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
